const routes = {
    REGISTER: '/register',
    LOGIN: '/login',
    TASKS: '/projects/:projectId/sprints/:sprintId',
    SPRINTS: '/projects/:projectId',
    PROJECTS: '/projects',
};

export default routes;
