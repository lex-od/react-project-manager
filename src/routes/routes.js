const routes = {
    REGISTER: '/register',
    LOGIN: '/login',
    TASKS: '/projects/:projectId/sprints/:sprintId',
    SPRINTS: '/projects/:projectId/sprints',
    PROJECTS: '/projects',
};

export default routes;
